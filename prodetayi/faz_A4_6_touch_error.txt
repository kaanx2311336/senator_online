FAZ A4-6 TAMAMLANDI: Touch Desteği + Error Handling + Polish

Yapılan Değişiklikler:
1. js/interaction/raycaster.js:
   - onClick ve onMouseMove fonksiyonlarına try-catch blokları eklendi.
   - Touch olaylarını (touchstart, touchmove) algılayacak şekilde `event.touches` ve `event.changedTouches` parametreleri işleme alındı.
   - onMouseMove fonksiyonuna requestAnimationFrame tabanlı bir throttle mekanizması eklendi (Performans artışı için).
2. js/interaction/cameraControls.js:
   - try-catch blokları eklendi.
   - OrbitControls için `touches` objesi (PAN, DOLLY_PAN) set edilerek mobil touch desteği onaylandı.
   - WebGL context lost durumu için `webglcontextlost` olay dinleyicisi eklendi; grafik belleği kaybolduğunda kullanıcıyı uyaran ve UI kaplaması sunan bir overlay entegre edildi.
3. js/interaction/selection.js & animations.js:
   - Kritik asenkron ve animasyon işlevleri try-catch bloklarıyla sarmalandı, olası render / load hataları engellendi.

GÖREV DURUMU: TAMAMLANDI (GECIS ONAY).
